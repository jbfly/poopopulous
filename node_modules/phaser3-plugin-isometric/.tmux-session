#!/usr/bin/env zsh

tmux new-session -s phaser-isometric -n nvim "nvim;zsh" \; \
     new-window -d -n "zsh" \; \
     new-window -d -n tig "tig" \; \
     new-window -d -n plugin-watch "yarn webpack --watch" \; \
     new-window -d -n docs-server "cd docs && yarn webpack-dev-server" \;
